function [Omega_deg, i_deg, omega_deg, theta_0_deg] = rad2deg(Omega, i, omega, theta_0)

Omega_deg = rad2deg(Omega);

i_deg = rad2deg(i);

omega_deg = rad2deg(omega);

theta_0_deg = rad2deg(theta_0);

end